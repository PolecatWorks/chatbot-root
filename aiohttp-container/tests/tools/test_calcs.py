import pytest
from customer.tools import count_calls, get_time

def test_sum_numbers():
    assert True
    # Test with positive numbers
    # assert get_time.invoke({"numbers": [1, 2, 3]}) == 6

    # # Test with negative numbers
    # assert count_calls.invoke() == 1
